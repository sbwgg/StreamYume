# Subtitle/Dubbed Options Enhancement

## Pending Tasks
- [ ] Update VideoService.listShows to filter TV shows based on episodes' isSubtitled/isDubbed instead of show's flags
- [ ] Update browse controller to use audioFilter instead of separate isSubtitled/isDubbed
- [ ] Update browse template to replace checkboxes with select dropdown for audio/subtitles (Any, Subbed, Dubbed, Both)
- [ ] Update applyFilters logic in browse controller to set params based on audioFilter value
